VITE_API_URL=http://localhost:3000/api
# URL publica del backend (para los QR de /wa/* y la descarga de documentos legales)
VITE_PUBLIC_BACKEND_URL=http://localhost:3000
# URLs de respaldo de los documentos legales (si no se obtienen de GET /legal/docs).
# El front prioriza el endpoint del backend; estas se usan solo como fallback.
VITE_TERMS_URL=
VITE_PRIVACY_URL=
